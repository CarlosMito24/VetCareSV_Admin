import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CrearnuevodminPage } from './crearnuevodmin.page';

describe('CrearnuevodminPage', () => {
  let component: CrearnuevodminPage;
  let fixture: ComponentFixture<CrearnuevodminPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearnuevodminPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
